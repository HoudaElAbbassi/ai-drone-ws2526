# AI Drone Project - Python Dependencies
# For Raspberry Pi Zero 2 WH with Raspberry Pi OS (Bullseye)

# Core Dependencies
numpy>=1.24.0,<2.0.0
pillow>=10.0.0

# Computer Vision
opencv-python>=4.8.0

# TensorFlow Lite (install via apt on Raspberry Pi)
# sudo apt install python3-tflite-runtime
# or: pip install tflite-runtime==2.14.0

# Google Coral Edge TPU (install via apt on Raspberry Pi)
# sudo apt install python3-pycoral
# or: pip install pycoral==2.0.0

# Raspberry Pi Camera (install via apt on Raspberry Pi)
# sudo apt install python3-picamera2

# Serial Communication (for GPS)
pyserial>=3.5

# MAVLink (optional, for autopilot communication)
pymavlink>=2.4.41

# Data Processing
pandas>=2.1.0

# Web Framework (optional, for dashboard)
flask>=3.0.0
flask-cors>=4.0.0

# Configuration
pyyaml>=6.0.0

# Logging
loguru>=0.7.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Development Tools
black>=23.0.0
flake8>=6.0.0
